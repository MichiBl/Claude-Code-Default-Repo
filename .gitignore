# --- Secrets & lokale Konfiguration — NIEMALS committen -----------------
.env
.env.*
!.env.example
secrets/
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
credentials*.json
service-account*.json
# .npmrc/.pypirc/.netrc können Auth-Tokens enthalten. Braucht ein Projekt eine
# committete, token-freie Registry-Config, die Zeile gezielt entfernen.
.npmrc
.pypirc
.netrc

# Lokale Claude-Code-Einstellungen (nur die geteilte settings.json committen)
.claude/settings.local.json

# --- Abhängigkeiten & Build-Artefakte ------------------------------------
node_modules/
dist/
build/
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage
coverage/

# --- Outputs & Logs -------------------------------------------------------
*.log
output/
tmp/

# --- OS & Editor -----------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
